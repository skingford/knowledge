---
title: AI、IM/直播与云原生架构：专项场景突破、架构与云原生
description: 聚焦 AI 工程化、IM/直播高并发、微服务治理和 Kubernetes 落地，补齐场景题与平台题主线。
---

# AI、IM/直播与云原生架构：专项场景突破、架构与云原生

> 返回总览：[AI、IM/直播与云原生架构](./ai-im-k8s-architecture-interview-guide.md)
>
> 上一篇：[基础认知与核心技术栈](./ai-im-k8s-architecture-interview-guide-foundation-and-core-stack.md)
>
> 下一篇：[软实力与高频追问题库](./ai-im-k8s-architecture-interview-guide-soft-skills-and-question-bank.md)

## 专项场景突破

这部分是 JD 里最容易做差异化的地方。你的回答必须从“会用组件”上升到“理解业务链路”。

### AI 工程化能力

#### 1. 支持流式输出的接口设计

必须能讲清：

- 为什么 AI 场景适合用 Streaming/SSE，而不是所有结果都等到最后一次性返回
- 服务端如何把模型 token、工具调用中间态、最终结论拆成不同事件
- 客户端断开、超时取消、幂等重试和落库策略怎么处理

常见接口分层：

- 同步请求接口：适合短请求、简单问答
- SSE 流式接口：适合对话、Agent 中间过程透传
- 异步任务接口：适合超长耗时、批处理、复杂工具链编排

::: details 标准答案：流式输出设计要点

**为什么用 SSE 而不是等全部结果**：LLM 推理 token 逐个生成，首 token 延迟可能只有几百 ms，但完整响应可能需要 10-30 秒。SSE 让用户在第一个 token 生成时就看到输出，体验从"等待"变成"阅读"，感知延迟大幅降低。

**服务端设计**：HTTP 响应头 `Content-Type: text/event-stream`，每个 event 包含 `event` 类型和 `data` 字段。建议区分事件类型：`token`（模型流式输出）、`tool_call`（工具调用开始/结束）、`error`（可恢复/不可恢复错误）、`done`（完成信号）。服务端维护一个 channel 接收模型输出，HTTP handler 用 `select` 同时监听 channel 和 `ctx.Done()`。

**断连处理**：客户端断开时 Go 的 `http.Request.Context()` 会被 cancel，服务端感知后应立刻停止模型推理和工具调用（通过 context 级联取消），避免浪费算力。客户端重连时通过 `Last-Event-ID` 或业务 session ID 恢复位置。

**幂等与落库**：流式响应的每个 event 带序号，最终完整响应在流结束后异步写入数据库。如果需要重试，客户端发送上次收到的最后序号，服务端从该位置续传。

:::


#### 2. 多模型调度与异步任务

高分回答要覆盖：

- 按模型、租户、接口类型做限流，不要只做全局 QPS 限流
- 失败时优先做快速失败、降级和 fallback，不要无限重试
- 长耗时任务进入异步队列，前台返回任务 ID，后台推进状态机
- 推理节点池要和普通 API 节点隔离，避免彼此争抢 CPU、内存和连接资源

图例参考：多模型、多工具并发时，最容易被忽略的是“不同下游必须有独立资源池”。这张舱壁隔离图可以直接拿来解释为什么 AI 推理、检索和普通业务 RPC 不能共用一个无界池子。

<GoMicroserviceDiagram kind="bulkhead-isolation" />

#### 3. 数据处理

常见考点：

- 数据清洗、切分、去重、脱敏、标签化
- 向量存储的基本概念，例如 embedding、索引构建、召回、重排
- RAG 和 Agent 不是一回事：RAG 解决检索增强，Agent 解决规划与工具编排

::: details 标准答案：RAG vs Agent 区分

**RAG（检索增强生成）**：用户提问 → 从向量库/全文索引中检索相关文档片段 → 把检索结果作为上下文拼接到 prompt → LLM 基于上下文生成答案。核心价值是减少幻觉、支持私有知识库。关键环节：文档切分策略（chunk size/overlap）、embedding 模型选择、召回数量与重排序（reranker）、prompt 模板设计。

**Agent（智能体）**：用户提问 → LLM 理解意图并制定计划 → 循环调用工具（搜索、数据库查询、API 调用、代码执行等） → 根据工具返回结果判断是否继续 → 生成最终答案。核心价值是自主推理和多步决策。关键环节：工具定义（schema）、规划策略（ReAct / Plan-and-Execute）、循环终止条件、安全边界。

**面试时的关键区分**：RAG 是"检索 + 生成"的单次管道，Agent 是"规划 + 执行 + 反馈"的多步循环。一个 Agent 内部可以调用 RAG 作为工具之一，但 RAG 本身不具备规划和多步推理能力。

:::


### IM/直播高并发架构

先补一个很容易混淆的边界：在线广播和可靠离线消息不是同一套模型。下面这张图先把“谁在线谁收到”的在线广播心智立住，再往下看长连接、房间 fan-out 和离线补偿。

<GoDataCacheDiagram kind="redis-pubsub" />

#### 1. 长连接管理

必须能讲清：

- 长连接网关和业务服务尽量拆开
- 连接状态、会话状态、用户在线状态不要混成一层
- 心跳、超时、断线重连、连接迁移和多端登录策略要提前定义

百万连接场景常见做法：

- 网关无状态化，用户连接通过一致性 hash 或会话路由分散到多个节点
- 用户在线状态放到独立存储层或状态服务，不直接绑在单机内存里
- 广播、房间消息、系统通知通过消息总线分发，不走点对点同步调用

::: details 标准答案：百万长连接架构

**分层架构**：接入层（长连接网关） → 路由层（用户/房间路由） → 业务层（消息处理、房间管理） → 存储层（消息持久化、在线状态）。网关只负责连接管理和协议编解码，不做业务逻辑。

**单机连接数上限**：Go 的 goroutine 模型天然适合长连接场景。单机 10 万连接时，每连接一个读 goroutine + 一个写 goroutine，内存开销约 2-4 GB（goroutine 栈 + 连接缓冲区）。瓶颈通常在内存和文件描述符（`ulimit -n`），而不是 CPU。用 epoll 优化（如 gnet、nbio）可以进一步减少 goroutine 数量。

**消息投递模型**：用户 A 发消息 → 业务层处理 → 查路由表找到用户 B 所在网关节点 → 通过内部 RPC 或消息总线推到目标网关 → 网关下推到用户 B 的连接。如果用户 B 不在线，消息写入离线存储，上线时拉取。

**心跳与断线重连**：客户端定时发心跳（通常 30-60 秒），超时未收到判定断连。断线重连时客户端带上 last_msg_seq，服务端补推缺失消息。多端登录通过 device_id 区分，踢下线通过路由层广播。

:::


#### 2. 消息即时性

必须能讲清：

- 写扩散适合群规模较小、读取极高频、要求极致读性能的场景
- 读扩散适合大群、超大房间、在线状态强波动场景
- IM 和直播房间消息并不一定用同一套投递模型

一个比较稳的表达是：

- 私聊和小群偏写扩散
- 大群、直播公屏、系统广播偏读扩散或在线用户实时 fan-out
- 明确”离线存储”和”在线推送”是两条链路，不要混成一个组件

::: details 标准答案：读扩散 vs 写扩散

**写扩散（Fanout-on-Write）**：消息发出时，服务端为每个接收者生成一份消息副本写入各自的收件箱。优点：读取极快，打开收件箱直接查自己的表。缺点：一条群消息如果群成员 500 人就要写 500 次，写放大严重。适合：私聊、小群（< 200 人）。

**读扩散（Fanout-on-Read）**：消息只写一份到群时间线。用户读取时从群时间线拉取未读消息。优点：写入量小。缺点：读取时需要合并多个群的时间线，读开销大。适合：大群（> 500 人）、直播间公屏、频道广播。

**混合方案**：实践中通常混合使用。私聊和小群用写扩散保证读性能；大群和直播间用读扩散降低写压力；系统广播用在线推送 + 离线拉取。

:::


#### 3. 稳定性治理

必须能讲清：

- 限流放在入口、房间、用户、下游多个层次
- 熔断的重点不是报错，而是阻止慢依赖把线程池、连接池和 goroutine 拖死
- 降级要提前定义，例如关闭进场动效、降低消息刷新频率、只保留核心互动链路

## 架构与云原生

这部分考的是你能不能既看得懂系统边界，又能把服务落到可运维的生产环境里。

### 分布式架构

#### 1. 微服务治理

需要能讲清这些能力的边界：

- 服务发现解决的是实例动态变化，不解决熔断和重试策略
- 配置中心解决的是运行时配置收敛，不解决发布流程混乱
- 链路追踪解决的是跨服务定位问题，不等于完整可观测性

回答时建议按这条线说：

- 服务边界如何拆
- 同步链路和异步链路如何组合
- 配置、注册发现、鉴权、灰度、观测如何接入
- 哪些公共能力沉到平台层，哪些保留在业务服务内

图例参考：微服务题最稳的回答顺序通常是“先能通信，再能稳住，最后能协作和治理”。先看总览图，再拆服务发现、熔断、追踪和灰度会更顺。

<GoMicroserviceDiagram kind="overview" />

::: details 标准答案：微服务治理核心能力

**服务发现**：服务启动时向注册中心（Consul / Nacos / etcd / K8s Service）注册实例地址，消费方从注册中心获取可用实例列表。K8s 环境下 Service + DNS 天然提供服务发现，通常不需要额外的注册中心。服务发现只解决"找到谁"，不解决"该不该调"（需要熔断）和"调失败了怎么办"（需要重试策略）。

**熔断**：当下游错误率或延迟超过阈值时，短路请求直接返回降级结果，防止故障扩散。状态机：Closed → Open（触发阈值）→ Half-Open（探测恢复）→ Closed。关键参数：窗口大小、失败率阈值、熔断持续时间、Half-Open 探测比例。

**链路追踪**：通过 TraceID + SpanID 串联一个请求经过的所有服务。OpenTelemetry 是当前标准。需要在 HTTP header / gRPC metadata / Kafka header 中传播 trace context。链路追踪只是可观测性的一部分，完整的可观测性 = Traces + Metrics + Logs。

**灰度发布**：按用户 ID / 设备 / 地域 / 百分比将流量路由到新版本。K8s 场景下通常结合 Istio VirtualService 或 Ingress 的 canary annotation 实现。灰度的重点不是技术实现，而是灰度指标（成功率、延迟、错误率）的自动化对比和自动回滚。

:::


#### 2. 高可用设计

常见考点：

- 单机故障、机房故障、跨地域故障分别怎么处理
- 数据库主从切换的 RPO/RTO 如何权衡
- 缓存预热、降级开关、只读模式、限流阈值如何提前准备

高分回答一般会强调：

- 高可用不是“做主从”就结束，而是故障发生时系统如何有序退化
- 真正重要的是演练、切换脚本、告警门槛和回滚路径

### Kubernetes 与交付

#### 1. Pod 生命周期和资源管理

必须能讲清：

- Pod 从调度、拉镜像、启动、探针通过到终止的关键阶段
- `requests` 决定调度保底，`limits` 决定可用上界
- AI 服务不能简单照搬普通 Web 服务的资源配置，因为推理负载波动和内存占用波动都更明显

比较稳的回答方式：

- 对在线推理服务，CPU/内存 `requests` 不要压得太低，否则节点会被过度装箱
- 对明显 CPU 密集或 GPU 密集型工作负载，优先做专用节点池和资源隔离
- HPA 不能只看 CPU，很多 AI 服务更应该看并发数、队列长度、P95 延迟或自定义业务指标

图例参考：Pod 生命周期里最容易讲混的是三类探针。先看清 `startupProbe` 负责保护慢启动，`readinessProbe` 决定能不能接流量，`livenessProbe` 才是长期存活检查。

<K8sDiagram kind="probe-gating-chain" />

图例参考：优雅终止也不能只背 `SIGTERM`。真正顺序是先摘流量，再执行 `preStop`，给应用处理存量请求的时间，超时后才会被 `SIGKILL` 强杀。

<K8sDiagram kind="pod-termination-timeline" />

图例参考：K8s 弹性不是只开 HPA，而是指标、调度、Pending、节点扩容和 Ready 接流量的一整条链。

<K8sDiagram kind="autoscaling-control-chain" />

::: details 标准答案：Pod 生命周期与资源管理

**Pod 生命周期关键阶段**：
1. **Pending**：调度器根据 requests 找合适节点 → 拉取镜像
2. **Running**：容器启动 → 执行 `postStart` hook → 等待 `startupProbe` 通过（防止慢启动被杀） → `readinessProbe` 通过后才接入 Service 流量 → `livenessProbe` 持续检测存活
3. **Terminating**：收到 SIGTERM → 执行 `preStop` hook → 优雅关闭期（默认 30s） → 超时后 SIGKILL 强杀

**requests vs limits**：
- `requests`：调度保底，调度器据此决定 Pod 放在哪个节点。设太低会导致节点过度装箱（overcommit），实际负载上来后互相争抢
- `limits`：硬上界。CPU limit 被超时会被 throttle（降速），memory limit 被超会被 OOMKilled
- **最佳实践**：CPU requests 设为实际 P50 用量，limits 设为 P99 的 1.5 倍或不设（burstable）；内存 requests ≈ limits（避免 OOM）

**AI 服务特殊性**：推理负载波动大（空闲时几乎不用 CPU，推理时吃满），内存占用高且稳定（模型常驻）。建议：内存 requests = limits = 模型大小 + buffer；CPU 用 burstable 策略；使用专用节点池 + taint/toleration 隔离。

:::


#### 2. Service 与流量入口

要能讲清：

- 集群内稳定访问依赖 `Service`
- 对外流量一般经过 `LoadBalancer`、Ingress、Gateway
- “固定出口 IP”“第三方白名单”“Pod 动态 IP”是三件不同的事

这是非常高频的追问点，尤其是远程岗位和云原生岗位。

图例参考：只要把“集群内访问我方服务”“外部访问我方服务”“我方访问第三方”这三种场景拆开，白名单题基本就不会答乱。

<K8sDiagram kind="ip-whitelist-patterns" />

::: details 标准答案：Service 与流量入口

**ClusterIP**：集群内部虚拟 IP，只在集群内可达。kube-proxy 通过 iptables/IPVS 规则将流量转发到后端 Pod。

**NodePort**：在每个节点的固定端口暴露服务，外部通过 `<NodeIP>:<NodePort>` 访问。端口范围 30000-32767，适合测试，生产不推荐直接用。

**LoadBalancer**：云厂商自动创建外部负载均衡器（如 AWS ELB、阿里云 SLB），是生产最常见的对外暴露方式。

**Ingress / Gateway API**：七层路由，支持域名、路径、TLS 终止。一个 Ingress Controller（如 Nginx Ingress、Istio Gateway）管理所有入口规则，避免每个服务都创建 LoadBalancer。

**固定出口 IP 问题**：Pod IP 是动态分配的。第三方 API 白名单需要固定 IP 时，方案是：配置 NAT 网关让集群出口流量走固定 IP，或使用 Istio Egress Gateway，或在专用节点上绑定弹性 IP。这和"固定 Pod IP"是完全不同的问题。

:::


#### 3. CI/CD

建议固定成一条流水线：

1. 代码提交后触发测试、构建、镜像扫描
2. 产出版本化 Docker 镜像
3. 通过 Helm、Kustomize 或 GitOps 系统发布到 K8s
4. 用探针、灰度和回滚机制保证上线风险可控

<GoCloudNativeDiagram kind="cloud-ci-cd" />
